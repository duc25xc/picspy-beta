{
    "name": "ducowo-mern-base",
    "version": "1.0.0",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "dev:be": "npm run dev --prefix backend",
        "dev:fe": "npm run dev --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}